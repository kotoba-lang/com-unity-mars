{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([unity-mars.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'unity-mars.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
